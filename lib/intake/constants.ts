// Shared between the "use server" actions file (which can't export plain
// constants) and any server component/helper that needs to read the cookie.
export const LEAD_COMPANY_COOKIE = "demo_lead_company_id";
